server:
  port: 8080
spring:
  datasource:
    driver-class-name: org.h2.Driver
    url: jdbc:h2:mem:testdb
  jpa:
    show-sql: true
    properties:
      hibernate.sql_format: true
    hibernate:
      ddl-auto: create